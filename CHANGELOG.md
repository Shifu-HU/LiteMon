# 更新日志

## v0.2.0

### 新增
- **7 种语言**：简体中文 / English / 日本語 / Français / Русский / Español / العربية
  （覆盖联合国全部 6 种官方语言 + 日语）。设置页改为**下拉菜单**，切换立即生效并停留在当前页，
  阿拉伯语自动切换 RTL 镜像布局。
- 设置页**配色方案名称随语言切换**。配色改用稳定 `Id`（`navy`/`matcha`/…）作持久化键，
  与显示名解耦；旧版存档里的中文名仍能正确解析，升级不会丢配色。
- **外接盘识别重写**，修复 USB 移动硬盘被误判为内置盘、因而不生成 DISK2 页签的问题
  （详见 README「外接盘识别」一节）。判定抽为纯函数 `DiskCollector.IsExternalBus`，附单元测试。

### 修复
- **磁盘读写速度永远显示 0 B/s**（重要）。`DiskCollector` 拼 PDH 计数器路径时重复加了冒号，
  生成 `\LogicalDisk(C::)\Disk Read Bytes/sec` 这种不存在的实例。`PdhAddEnglishCounterW`
  对无效实例是**惰性接受**的（仍返回成功），直到 `PdhCollectQueryData` 才整体报
  `PDH_NO_DATA (0x800007D5)`，于是所有盘读数恒为 0。现在盘符冒号不再重复拼接，
  且 Collect 失败会立即作废查询、下一帧重建（而不是干等 20 秒）。
- **WIFI 页协议被裁切看不全**（"802.11ax (Wi-Fi 6)"）。摘要卡统一用 21px 粗体，
  实测该文本宽 193px，而单卡可用宽仅 136px——**溢出 57px**。协议卡改用 13.5px
  （实测 124px，余量 11px），并开启换行；卡片内边距同步收紧。
- 设置页语言下拉浮层**背景透明**——模板引用了不存在的资源键 `Brush.Panel`，改用 `Brush.MenuBg`。
- 切换语言后**页面闪回 CPU**、且需点击其它按钮才能重进设置页。
- **DISK2/DISK3 页签排到导航栏最右侧**（贴着设置齿轮），改为紧跟 DISK1 依次排列。
- **容量为 0 的读卡器空槽位**会生成一个空的 DISK2/DISK3 页签，现已过滤。
- WIFI 页协议显示为 `—`（`dot11_phy_type` 映射缺少 10/11）。
- 笔记本**合盖再开盖触发 `InvalidCastException`** 导致崩溃。
- DISK 读写曲线恒为 0（1 秒快照与 0.1 秒曲线共用同一个 PDH 查询）。

---

## v0.1.0

首个版本：CPU / GPU / 内存 / 显存 / 磁盘 / 网络实时监视，任务管理器式主窗口，玻璃拟态主题，MIT 开源。
